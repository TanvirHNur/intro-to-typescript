function add1 (num1: number, num2:number ):number{
    return num1 + num2;
}

const add2 = (first:number, second: number):number => first + second;

// no parameter\
const add3 = (first: number, second: number):void => {
    console.log(first *3)
}